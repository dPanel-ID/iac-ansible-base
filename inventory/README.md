# Inventory Templates

dPanel creates real workspace inventory paths when a personal or organization
workspace is initialized.

Do not commit generated inventory that contains real hosts, usernames, IP
addresses, ports, tokens, or secret references.

Safe template examples live under:

```text
inventory/templates/
```

